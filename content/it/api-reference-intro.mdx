---
title: API Reference
description: Documentazione completa dell'API REST per Tratto Email
---

Documentazione completa dell'API REST per Tratto Email. Tutti gli endpoint richiedono autenticazione con una chiave API.

## URL di Base

```
https://api.tratto.email/v1/
```

## Autenticazione

Includi la tua chiave API come bearer token:

```http
Authorization: Bearer tratto_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

## API Reference Interattiva

Per documentazione dettagliata degli endpoint, esempi di richiesta/risposta, e un explorer API live, visita il [**riferimento interattivo**](/it/docs/api-reference).

Il riferimento interattivo include:
- **Documentazione completa degli endpoint**: Metodo, percorso, parametri, schema richiesta/risposta
- **Requisiti di autenticazione**: Quali endpoint richiedono la chiave API
- **Codici di errore**: Tutte le possibili risposte di errore
- **Pannello Try it**: Fai richieste API reali dalla documentazione (la tua chiave API rimane nel browser)
- **Esempi di codice**: cURL, JavaScript, Python

## Link Rapidi

- [Invia Email](/it/docs/send-email): POST /emails
- [Status Email](/it/docs/email-status): `GET /emails/{id}`
- [Domini](/it/docs/domains): Verifica dei domini
- [Template](/it/docs/templates): Template di email
- [Contatti](/it/docs/contacts): Gestione contatti
- [Webhook](/it/docs/webhooks): Sottoscrizioni agli eventi
- [Analytics](/it/docs/analytics): Metriche e statistiche

## Rate Limiting

Tutte le richieste sono limitate a **500 per minuto** per chiave API. Vedi [Rate Limits](/it/docs/rate-limits) per dettagli.

## Gestione Errori

Tutti gli errori restituiscono JSON con codice e messaggio:

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or missing API key",
    "docs": "https://docs.tratto.email/docs/authentication"
  }
}
```

Vedi [Error Codes](/it/docs/error-codes) per il riferimento completo.

---

👉 **Inizia dal [riferimento API interattivo](/it/docs/api-reference)** per esplorare tutti gli endpoint.
